<%- include('partials/header') %>
<section class="pt-20 bg-gradient-to-b from-gray-900 to-dark px-4">
  <div class="container mx-auto max-w-screen-xl min-h-screen pt-20">
    <h2 class="text-5xl font-bold text-center mb-16 text-white" data-aos="fade-up">Próximos Eventos</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% events.forEach((event, index) => { %>
        <div class="bg-dark rounded-lg shadow-lg overflow-hidden transform hover:-translate-y-2 hover:shadow-2xl transition duration-300" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
          <div class="relative">
            <img src="<%= event.image %>" alt="<%= event.title %>" class="w-full h-64 object-cover">
            <div class="absolute top-0 right-0 bg-primary text-white font-bold px-4 py-2 rounded-bl-lg">
              <%= new Date(event.date).toLocaleDateString('pt-BR', { month: 'short', day: 'numeric' }) %>
            </div>
          </div>
          <div class="p-6">
            <h3 class="text-2xl font-semibold mb-4 text-white"><%= event.title %></h3>
            <p class="flex items-center mb-2 text-gray-300">
              <i class="far fa-clock mr-2 text-primary"></i>
              <%= new Date(event.date).toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' }) %>
            </p>
            <p class="flex items-center mb-4 text-gray-300">
              <i class="fas fa-map-marker-alt mr-2 text-primary"></i>
              <%= event.location %>
            </p>
            <p class="text-gray-400 mb-6 line-clamp-3"><%= event.description %></p>
            <div class="flex items-center justify-between">
              <a href="/events/<%= event.id %>" class="inline-block bg-primary text-white px-6 py-2 rounded-full font-bold text-sm hover:bg-secondary transition duration-300">
                Saiba Mais
              </a>
              <div class="flex space-x-4">
                <a href="https://www.instagram.com/bailedaakyam" class="text-primary hover:text-secondary transition duration-300" aria-label="Instagram">
                  <i class="fab fa-instagram fa-2x"></i>
                </a>
                <a href="https://wa.me/+5519999880296" class="text-green-500 hover:text-green-400 transition duration-300" aria-label="WhatsApp">
                  <i class="fab fa-whatsapp fa-2x"></i>
                </a>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<section class="pb-20 bg-gradient-to-b from-dark to-gray-900">
  <div class="container mx-auto px-4 text-center">
    <h2 class="text-4xl font-bold mb-8" data-aos="fade-up">Quer DJ AKYAM no seu evento?</h2>
    <p class="text-xl mb-12 max-w-2xl mx-auto" data-aos="fade-up" data-aos-delay="100">
      Traga a energia contagiante de DJ AKYAM para o seu próximo evento ou festival. 
      Entre em contato agora e faça seu evento inesquecível!
    </p>
    <a href="/contact" class="inline-block bg-primary text-white px-10 py-4 rounded-full font-bold text-lg hover:bg-secondary transition duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-1" data-aos="fade-up" data-aos-delay="200">
      Contate-nos
    </a>
  </div>
</section>
<%- include('partials/footer') %>